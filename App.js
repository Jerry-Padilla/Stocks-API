import React from "react";
import styled from "styled-components";

stocksApi = "";

async function FetchStonkInfo(stocksApi) {
  const response = await fetch();
  stonksData = await response.json();
}
